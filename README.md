tecnologie usate:
  springboot con framework hibernate,
  mysql con annesso mysqlbench per le query e conenssione al db,
  intellij per scrivere il codice e lancio del backend,
  postman per test dei servizi creati,
  source tree per gestione versioni su git
